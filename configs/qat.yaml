model:
  num_classes: 5
  classes: ["concrete", "brick", "metal", "wood", "stone"]
data:
  data_dir: "data/synthetic/"
qat:
  backend: "fbgemm"
  fine_tune_epochs: 10
  lr: 0.00001
  prune_ratio: 0.0 # Just QAT for this config
  prune_target_blocks: []
checkpointing:
  output_dir: "checkpoints/"
